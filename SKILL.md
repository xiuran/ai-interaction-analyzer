---
name: ai-interaction-analyzer
description: |
  读取你本地的 AI 对话记录（Claude Code / Codex / Qoder / Cursor），
  自动发现"AI 反复踩的坑"和"你已经验证有效的协作方式"，
  直接生成可安装到 CLAUDE.md / AGENTS.md / .cursorrules 的规则——装上就生效，不用自己总结。
  分析范围覆盖编码、排查、文档、方案设计、知识沉淀等所有类型的 AI 协作。
  触发："/ai-trace"、"分析我的 AI 对话"、"AI 协作分析"、"prompt 质量"、"看看我和 AI 聊天的问题"。
---

# AI Interaction Analyzer

从你真实的 AI 对话中（编码、排查、写文档、方案设计、知识沉淀……所有场景），挖掘出 AI 反复犯的错误模式和你已经验证有效的协作方式，产出可一键安装的规则——让 AI 不再重复踩坑。

---

## 触发条件

出现以下意图时触发：
- "分析一下我的 AI 对话质量"
- "最近我的 prompt 质量怎么样"
- "/ai-trace"
- "分析一下我用 Cursor / Claude Code / Codex 的情况"

---

## 前置检查

| 检查项 | 通过条件 | 失败处理 |
|--------|---------|---------|
| 至少一个数据源存在 | 见"支持的数据源" | 告知用户各数据源的检测状态 |
| Python 3.8+ 可用 | `python3 --version` | 运行 `setup.sh` 自动安装 |

---

## 支持的数据源

| 工具 | 支持程度 | 数据路径 | 分析内容 |
|------|---------|---------|--------|
| Claude Code | 完整支持 | `~/.claude/` | prompt 历史 + 完整 session 上下文 + 模型统计 |
| Codex | 完整支持 | `~/.codex/` | 历史 + rollout 上下文 + 模型统计 |
| Qoder | 完整支持 | `~/.qoder/` | JSONL transcript + 模型信息 |
| Cursor | 尽力解析 | `~/Library/.../Cursor/` | SQLite 数据库（schema 跨版本不稳定） |

<HARD-GATE>
纯本地只读分析。不上传数据，不修改用户数据。
</HARD-GATE>

---

## 执行流程 — 多轮循环分析

整体分 4 个阶段。阶段 2-3 循环执行直到没有新发现。

### 阶段 0：确定分析范围

```bash
python3 <SKILL_DIR>/scripts/analyzer.py --mode=scope
```

脚本扫描数据源，返回各时间范围的数据量。

<HARD-GATE>
报告各工具 prompt 数时，用 `source_counts_60d`（实际会被分析的条数），
或 `data_sources.*.prompt_count`——两者口径一致。
**不要**用 Codex 的 `history_index_count`——那只是 history.jsonl 索引，
Codex 几乎不写它（可能只有几十条），真实对话在 rollout transcript 里。
用错会让用户误以为"数据变少了"。
</HARD-GATE>

范围规则（首条命中即止）：
1. 用户明确要求"全部日期/全量/所有数据" → 不传 `--days` 参数（分析全部数据）
2. 近 60 天 prompt > 3000 → 缩小到 30 天（数据太多影响质量）
3. 近 60 天 prompt < 300 → 扩大到 180 天（数据太少提取不出模式）
4. 否则 → 60 天

告知用户分析范围后开始。

注意：`--days` 不传时 analyzer.py 默认分析全部数据（不做时间过滤）。

### 阶段 1：Layer 1 — 扫描输入，提取信号

```bash
python3 <SKILL_DIR>/scripts/analyzer.py --mode=signals --days=<N>
```

脚本扫描全部 prompt 文本，按类别提取：
- **挫折信号**：用户纠正 AI（编造 / 做错 / 不认真 / 越权 / 重复犯错）
- **正面信号**：用户认可 AI（夸赞 / 信任委托 / 复用方案）
- **成功 session**：≤ 5 轮，无否定词

输出：待深入分析的 incident 列表 + 值得提炼的成功 session 列表。

### 阶段 2：Layer 2 — 读取上下文，深度分析（循环）

对每个 incident / 成功 session，读取投诉点/成功点前后 ±4 轮完整对话：

```bash
python3 <SKILL_DIR>/scripts/analyzer.py --mode=context --session=<SID> --prompt="<TEXT>"
```

AI 读取上下文后分析：
- **AI 做了什么**：工具调用、输出内容
- **为什么失败/成功**：根因
- **用户如何纠正/为什么认可**：纠正方向
- **是否需要继续深入**：这个 incident 是否关联其他 session？

<HARD-GATE>
上下文读取限制在投诉点/成功点前后 ±4 轮（共 8 轮）。
禁止读取整个 session，避免 token 爆炸。
</HARD-GATE>

### 阶段 3：继续或停止（循环出口）

每轮 Layer 2 结束后判断是否继续：

优先级规则（首条命中即止）：
1. 发现了未覆盖的新问题模式 → 继续，读取更多 incident
2. 同一类问题跨多个项目出现 → 继续，对比差异
3. 当前 incident 关联到更早/更晚的 session → 继续，追踪关联
4. 已分析的 incident 覆盖了所有主要问题类型 → 停止
5. 连续 2 轮没有新发现 → 停止

停止后进入阶段 4。

### 阶段 4：综合输出

基于所有轮次的分析，产出完整诊断：

1. **概览仪表盘**：健康度评分、问题分布图、session 分布
2. **工具 & 模型分析**：按工具和模型双层展示问题率
3. **Incident 深度分析**：上下文 + 根因 + 提炼规则（按根因模式分组）
4. **成功模式分析**：从成功 session 中提炼 Best Practices
5. **维度拆分**：按项目/任务类型拆分（仅差异显著时展示）
6. **AI 协作指南**：综合所有分析维度，输出一段可直接复制使用的文本（核心产出）
7. **协作优化建议**：给用户的操作建议，解决规则管不到的问题
8. **Quick Install**：安装到各工具的配置文件

---

## 分析维度

| # | 维度 | 数据来源 | 输出 | 状态 |
|---|------|---------|------|------|
| 1 | 工具 & 模型分析 | session JSONL（message.model） | 按工具 + 按模型双层分析 | ✅ |
| 2 | 挫折信号分类 | prompt 文本 | 问题分布图 + incident 列表 | ✅ |
| 3 | 正面信号提取 | prompt 文本 + session JSONL | 成功模式 + Best Practices | ✅ |
| 4 | Incident 深度分析 | session JSONL（±4 轮上下文） | 根因 + 可溯源的 Prompt Rules | ✅ |
| 5 | 维度拆分 | 交叉表 | 按项目/任务类型/模型拆分（仅差异显著时） | ✅ |
| 6 | 高频短语 | prompt 文本 | 用户反复出现的短语 → 工作流特征 | ✅ |
| 7 | 效率数据 | session 统计 + 否定关键词 | 一次成功率 / 否定率 / 效率分 | ✅ |
| 8 | AI 行为质量 | session JSONL（tool_use） | Read:Edit 比 / 不读就写比率 | 🔜 规划中 |
| 9 | 协作画像 | prompt 文本 | 任务类型分布 + 协作风格分布 | ✅ |
| 10 | Prompt 信息完整度 | prompt 文本 | 五维评分 + 中性分档 + 欠信息率 | ✅ |
| 11 | Session 反模式检测 | session 统计 | 否定循环 / 过短命令 / 膨胀 / 漂移 | ✅ |
| 12 | 正面信号分析 | prompt 文本 | 内部消费 → 喂入 AI 协作指南的用户画像 | ✅ |
| 13 | 用户认知模式 | prompt 文本 | 内部消费 → 喂入 AI 协作指南的用户画像 | ✅ |
| 14 | 判断框架提炼 | incident 根因 + 正面模式 | 内部消费 → 喂入 AI 协作指南的判断原则 | ✅ |

---

## 输出节点决策规则

| 节点 | 输出条件 | 跳过条件 |
|------|---------|---------|
| 仪表盘（健康度 + 分布 + session） | 始终输出 | 不跳过 |
| 工具 & 模型分析 | ≥ 2 个工具或 ≥ 2 个模型 | 只有单一工具 + 单一模型 |
| Incident 分析 | ≥ 1 个有上下文的 incident | 无 incident |
| 成功 session 分析 | ≥ 1 个有上下文的成功案例 | 无成功案例 |
| 维度拆分 | 维度间差异显著 | 差异不显著 |
| Prompt 质量 & 效率 | prompt_quality 字段非空 | 不跳过 |
| Session 反模式 | session_antipatterns.total_detected > 0 | 无反模式 |
| AI 协作指南 | ≥ 100 条 prompt + ≥ 1 个 incident 有上下文 | 数据量太少 |
| 协作优化建议 | 存在用户侧可改进的协作行为 | 所有问题都可通过 AI 规则解决 |
| Quick Install | 有 AI 协作指南输出时 | 无产出 |

---

## 输出模板

直接在对话中输出。匹配用户的语言（中文或英文）。

重要：analyzer 输出的 `category_labels` JSON 包含 `en` 和 `zh` 两种显示名。
始终使用 `category_labels` 中的可读标签，**禁止**显示原始 key 如 `low_effort` 或 `fabrication`。

必须使用以下结构：

<HARD-GATE>
健康度必须直接取 overview.health_score（analyzer 已确定性算好），
禁止自己用纠正率/否定率现算——否则同一份数据每次跑出的分数会飘。
进度条按 health_score 填充：每 5% 一格，共 20 格。
</HARD-GATE>

```
╔══════════════════════════════════════════════════════════════════════════╗
║                                                                          ║
║                        AI Interaction Analyzer                           ║
║                        ── 协 作 质 量 诊 断 ──                            ║
║                                                                          ║
║    📅  {start} ~ {end} ({N}天)                                           ║
║    💬  {prompts} prompts · {sessions} sessions                           ║
║    🔧  {tool (count)} · {tool (count)}                                   ║
║                                                                          ║
║    ┌──────────────────────────────────────────────────────────┐          ║
║    │                                                          │          ║
║    │   健 康 度    {bar}  {health_score}%                       │          ║
║    │   （无纠正的 session 占比）                                │          ║
║    │                                                          │          ║
║    │   🔴  {n} 次纠正       🟢  {n} 次一次成功                  │          ║
║    │   📊  {n} 个否定循环 session                               │          ║
║    │   📝  {n} 条已验证规则 + {n} 条深度发现                    │          ║
║    │                                                          │          ║
║    └──────────────────────────────────────────────────────────┘          ║
║                                                                          ║
╚══════════════════════════════════════════════════════════════════════════╝

--- 1. 工具 & 模型分析 ---

<HARD-GATE>
必须同时展示"按工具"和"按模型"两个子表。
analyzer.py --mode=analyze 输出的 model_stats 字段包含每个模型的 prompt 数和问题率，
不要跳过，必须渲染成表格。如果 model_stats 为空，显式说明"无模型数据"。
</HARD-GATE>

分两层展示：先按工具，再按模型。柱状图用 █ 和 ░：

┌───────────────────────────────────────────────────────────────────────────┐
│  ──── 按工具 ──────────────────────────────────────────────────────────── │
│  工具            总 prompt   问题数   问题率   主要问题                     │
│  Claude Code       1200       120    10.0%   做错(40) 不认真(35)          │
│  ████████████████████████████████████████████████████████  1200           │
│  Codex               25         3    12.0%   不认真(1) 编造(1)            │
│  █░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  25            │
│  ...                                                                      │
│                                                                           │
│  ──── 按模型 ──────────────────────────────────────────────────────────── │
│  模型                     总 prompt   问题数   问题率   来源              │
│  claude-opus-4-8             106        4     3.8%   Claude Code         │
│  ██████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  106          │
│  gpt-5.5                 27        1     3.7%   Codex               │
│  ██░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  27           │
│  claude-opus-4-7             400       36     9.0%   Claude Code         │
│  █████████████████████████████████░░░░░░░░░░░░░░░░░░░░░░░░  400          │
│  claude-opus-4-6             790       79    10.0%   Claude Code         │
│  ████████████████████████████████████████████████████████░░  790          │
│  ...                                                                      │
│                                                                           │
│  💡 发现                                                                   │
│  · 工具间差异 vs 模型间差异：哪个因素影响更大                              │
│  · 新模型 vs 旧模型的问题率对比                                            │
│  · 各模型/工具的共性问题和独有问题                                         │
└───────────────────────────────────────────────────────────────────────────┘

--- 2. 问题概览 ---

用 box drawing 画表格。三个子区块。柱状图直接用 █ 和 ░：

┌───────────────────────────────────────────────────────────────────────────┐
│  ──── 问题类型 ────────────────────────────────────────────────────────── │
│   AI 不够认真    ███████████████████████████████████████████   40         │
│   AI 编造/瞎猜   ██████████████████████████░░░░░░░░░░░░░░░░   25         │
│   AI 做错了      ████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░   15         │
│   ...                                                                     │
│                                                                           │
│  ──── Session 长度 ────────────────────────────────────────────────────── │
│   1-5 轮       ████████████████████████████████████████████   92   63%   │
│   6-15 轮      ██████████████████░░░░░░░░░░░░░░░░░░░░░░░░░   38   26%   │
│   16-30 轮     ██████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   12    8%   │
│   30+ 轮       ██░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░    5    3%   │
│                                                                           │
│  ──── 按项目（归一化问题率，差异显著时展示）──────────────────────────── │
│   项目            总prompt  问题数  问题率  主要问题                       │
│   projectA          80      12    15.0%  编造+不认真                      │
│   projectB          60       6    10.0%  做错+编造                        │
│   ...                                                                     │
└───────────────────────────────────────────────────────────────────────────┘

--- 3. Prompt 质量 & 效率 ---

analyzer.py --mode=analyze 输出的 prompt_quality、efficiency、task_type_distribution、
collab_style_distribution、session_antipatterns 字段包含以下数据，必须渲染。

┌───────────────────────────────────────────────────────────────────────────┐
│  ──── Prompt 信息完整度（形式层面，满分 100）──────────────────────────── │
│                                                                           │
│   平均分：{avg_score}/100                                                 │
│                                                                           │
│   信息充分（80-100） ████████████████████████████████████  {n}   {%}%  │
│   基本够用（60-79）  ██████████████████████░░░░░░░░░░░░░░  {n}   {%}%  │
│   偏简（40-59）      █████████████░░░░░░░░░░░░░░░░░░░░░░░  {n}   {%}%  │
│   极简（0-39）       ███░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  {n}   {%}%  │
│                                                                           │
│   ⚠ 这衡量的是「形式上给了 AI 多少信息」，不是「prompt 好坏」。            │
│     简短指令 / 斜杠命令 / 口语追问本身没问题，「极简」不等于「差」。       │
│                                                                           │
│   欠信息率：{underspecified_rate}%（长度>10 但没有文件引用和目标动词）     │
│                                                                           │
│  ──── 效率三要素 ──────────────────────────────────────────────────────── │
│                                                                           │
│   一次成功率   {first_success_rate}%  ≤3 轮 + 无否定                      │
│   否定率       {negation_rate}%      含否定关键词的 session 占比           │
│   效率分       {efficiency_score}/100                                      │
│                                                                           │
│  ──── 任务类型分布（task_type_distribution）─────────────────────────────  │
│   编码  ████████████████████████████████████████████  {n}                  │
│   调试  ██████████████████░░░░░░░░░░░░░░░░░░░░░░░░░  {n}                  │
│   设计  ████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░  {n}                  │
│   调研  ██████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  {n}                  │
│   写作  ██████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  {n}                  │
│   配置  ███░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  {n}                  │
│   其他  █████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  {n}                  │
│   （其他 = 会话追问/纠正等无明确任务动词的 prompt，不是分类失败）           │
│   （另有 {unclassifiable_pasted} 条纯粘贴内容无法分类，已排除在分母外）     │
│                                                                           │
│  ──── 协作风格 ────────────────────────────────────────────────────────── │
│   委托式（帮我/给我）   {n}  {%}%                                         │
│   协作式（一起/讨论）   {n}  {%}%                                         │
│   审查式（检查/review） {n}  {%}%                                         │
│   指令式（直接指令）    {n}  {%}%                                         │
│                                                                           │
│  💡 发现                                                                   │
│  · 根据数据得出的 2-3 条具体发现                                          │
└───────────────────────────────────────────────────────────────────────────┘

--- 4. Session 反模式 ---

analyzer.py --mode=analyze 输出的 session_antipatterns 字段。
仅在 total_detected > 0 时展示。

┌───────────────────────────────────────────────────────────────────────────┐
│  ──── 检测到的用户侧反模式 ────────────────────────────────────────────  │
│                                                                           │
│   否定循环（连续 ≥2 轮否定）     {n} 个 session                           │
│   过短命令（≥3 条 <10 字符）     {n} 个 session                           │
│   Session 膨胀（>50 轮）         {n} 个 session                           │
│   目标漂移（任务类型切换 ≥3）    {n} 个 session                           │
│                                                                           │
│  ──── 受影响的 session ────────────────────────────────────────────────── │
│   项目        反模式类型       轮次   建议                                │
│   projectA    否定循环          12    第 2 次否定时停下来重写 prompt       │
│   projectB    Session 膨胀      65    拆成多个 session，一个目标一个        │
│   ...                                                                     │
│                                                                           │
│  参考：references/反模式库.md 的 1-4 章（用户侧）详细说明                 │
└───────────────────────────────────────────────────────────────────────────┘

--- 5. Incident 深度分析 ---

<HARD-GATE>
analyzer 输出三个跨 session 聚合字段，必须优先使用：
- 每个 incident 的 `matched_antipatterns`：命中 references/antipatterns.json 的已知 AI 侧模式。
  非空时，**直接复用其 root_cause 和 fix_rule**（这些是经过验证的高质量根因），
  不要自己重推一个更浅的版本。
- 顶层 `antipattern_library`：对全部挫折信号的库匹配结果，按根因聚合，含每个模式的
  count / project_count / session_count / span_days / projects / 现成 fix_rule。
  这是"按根因跨 session 分组"的现成骨架——已知模式直接用它，不要重新归纳。
- 顶层 `recurring_categories`：全部挫折信号按类别的粗粒度聚合（count / project_count /
  session_count / span_days），覆盖没匹配上已知模式的部分，用于"反复主题"视图。
</HARD-GATE>

<HARD-GATE>
两类计数不能混：
- `antipattern_library` 的 count = 命中某条**具体模式**的信号数（精确，带现成 fix_rule）
- `recurring_categories` 的 count = 某个**粗类别**（编造/做错…）的信号数（大，需深挖）
一个类别（如"编造"44 条）≠ 某条具体模式（如 ai_fabricate_api 5 条）。
[现成规则] 行**必须**填 antipattern_library 的真实 count/project_count/span_days，
禁止写"—"（那几行恰恰是最该优先装规则的）。
</HARD-GATE>

先输出「反复病根排行」（跨 session 聚合的核心价值），再做 incident 深挖：

┌───────────────────────────────────────────────────────────────────────────┐
│  ──── 反复病根（按影响范围排序）──────────────────────────────────────── │
│  根因                        次数 项目 跨度  处置                          │
│  ─ 已知模式（有现成规则）─────────────────────────────────────────────    │
│  前端/视觉不验证就交付         7    2  38天 [规则] 改完必须看渲染          │
│  输出用抽象代号看不懂          6    4  24天 [规则] 禁用代号提问            │
│  信息不足时编造 API/字段       5    2  20天 [规则] 调API前先读源码         │
│  ─ 粗类别（需 incident 深挖）─────────────────────────────────────────    │
│  AI 不够认真（浅执行）        77   18  52天 [深挖] 见模式 A               │
│  AI 做错了                    65   19  52天 [深挖] 见模式 B               │
│                                                                           │
│  💡 跨项目 + 长跨度 = 系统性问题，优先根治。上半区已有现成规则可直接装     │
└───────────────────────────────────────────────────────────────────────────┘

然后做 incident 深挖。不要逐个列举 incident，而是：
1. 先用 `antipattern_library` 覆盖所有能匹配到已知模式的 incident（复用现成根因+规则）
2. 剩下未命中的 incident，读上下文自行按根因分组（如"误解用户意图"等库里没有的新模式）
3. 每个模式命名（模式 A、B、C...）并附描述性标题
4. 每个模式下展示 2-3 个真实案例，包含完整对话上下文（含 tool_results 时，展示 AI 实际改了什么）
5. 跨项目广、时间跨度长的模式优先展示（project_count / span_days 高的排前面）

<HARD-GATE 样式>
从这一段起（5、5b、6、7）**不要用整框 ┌─┐**——中文双宽字符容易撑破右边框。
改用轻量卡片：`▎标题` + 缩进内容 + `┄` 细分隔。
引用原话只留最有信息量的一句，不要大段引用。
每个 incident 案例写成完整句子，不要为了凑行宽而截断到语义不通。
</HARD-GATE>

每个模式的格式（轻量卡片，非整框）：

▎模式 A — 一句话标题
  涉及 N incident · projA/projB · 跨 M 天
  ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄
  案例 1 · projA
    👤 用户纠正原话（一句话，保留完整语义）
    🤖 AI 的具体错误行为（一句话）
  案例 2 · projB
    👤 …
  ▸ 根因：一句话
  ▸ 场景：什么时候容易出现

--- 5b. 成功 session 分析 ---

关键要求：不要逐个列举 session。而是：
1. 分析所有成功 session（≤5 轮，无否定词）
2. 按可复用模式分组并命名
3. 每个模式展示一个真实的首条 prompt 示例，说明为什么有效

格式（轻量卡片）：

▎✅ 模式 A — 模式名（约占成功 N%）
  "真实首条 prompt（≤40 字，长则截断…）"
  → 为什么有效：让它一次成功的要素（≤30 字）

--- 6. AI 协作指南（核心产出，给 AI 读） ---

<HARD-GATE>
这段文本是给 AI 读的，不是给人看的报告。
判断标准：用户把这段复制到任何 AI 的 system prompt / CLAUDE.md / AGENTS.md 里，
这个 AI 就能比没读过的 AI 表现更好。

只放 **AI 能执行的内容**：用户画像（让 AI 知道怎么配合）、判断原则（怎么权衡）、行为规则（什么不能做）。
"高效模式""翻车场景"等用户侧的内容不要放这里——AI 控制不了用户怎么给指令。那些放在段落 7 协作优化建议里。
</HARD-GATE>

综合 positive_signals、cognitive_patterns、incident 根因、协作风格等数据，
产出一段**自包含的纯文本**。放在 markdown code block 里方便用户整段复制。

包含 3 个区块：

```
## AI 协作指南（auto-generated by ai-interaction-analyzer, {date}）

### 关于这个用户
用 bullet 描述用户的协作特征（第三人称），每条一行。
内容来自 cognitive_patterns + collab_style + positive_signals。
包含：什么对他重要、他怎么给指令、什么时候满意/不满意。

### 判断原则
用表格（原则 | 判断标准 | 适用场景）。
从 incident 根因和成功模式中归纳，比规则更深一层。3-5 条。

### 行为规则
编号列表，每条一行，具体到可执行。从 incident 根因中溯源。
```

格式要求：
- 用 **bullet + 表格** 代替大段文字，扫一眼就能抓到重点
- 整段放在 markdown code block 里，用户直接复制
- **自包含**——不引用其他段落，直接写清楚内容
- 总长度 **30-50 行**，精炼
- 判断原则表格：原则列简短（2-4字），判断标准和适用场景正常写完整句子
- 行为规则：每条完整写在一行或两行内，不要为凑列宽截成碎片

<HARD-GATE>
AI 协作指南中**禁止出现中间态统计数据**（百分比、频次、占比、"每 N 条"）。
AI 读到"72%"不能改变任何行为。只写结论——AI 读完后能直接改变行为的句子。
❌ "指令式沟通（72%），不给很多背景"
✅ "习惯直接下指令，很少给背景——你需要自己读代码补上下文"
❌ "每 13 条 prompt 就有 1 条要求沉淀"
✅ "极度重视可复用性——一次性消费的产出 = 低价值"
统计数据放在段落 1-5 的分析报告里给人看。段落 6 只放结论。

禁止泛泛的通用建议。每条规则/原则必须包含**从真实 incident 中来的具体细节**。
❌ "先读后写"（谁都知道）
✅ "新增枚举/错误码/配置时，先看同类的已有实现怎么注册的，
   按同样的方式注册到所有相关文件"（从"只注册了 1 个文件漏了 2 个"的 incident 中来）
❌ "不确定就说不确定"（废话）
✅ "调用不熟悉的 API 时，必须先查源码或文档确认方法签名，
   禁止凭记忆写——编造一次的修复成本是直接查的 10 倍"（从"编造不存在的方法名"的 incident 中来）

判断标准：**这条规则要有从 incident 里来的具体场景描述，但不能绑定特定技术栈，让任何开发者都能看懂。**
</HARD-GATE>

--- 7. 协作优化建议（给用户看） ---

这部分面向用户，分三类：你的高效区（保持）、你的翻车区（注意）、可操作的改进手段（执行）。
每条建议必须溯源到具体数据或 incident 模式。

产出分三个子区块。不用整框，用 `▎小标题` + 缩进。表格保持紧凑但内容写完整。

▎高效区（保持）— 从成功 session/正面信号提炼
  用 3 列窄表：模式 | 成功率 | 关键要素（每格 ≤10 字）
  一句话收尾：这些方式效果最好，继续用

▎翻车区（注意）— 从 incident 根因/项目问题率提炼
  用 3 列窄表：场景 | 原因 | 怎么避（每格 ≤10 字；问题率并进"场景"括号里）

▎可操作改进（执行）— 每条一行，格式：`[类型] 动作（溯源）`
  类型取自：Rules / Hooks / Skill / MCP / 模型 / 上下文

--- 8. Quick Install ---

先展示配置路径表格，然后**用 AskUserQuestion 询问用户想怎么安装**。
用户可以选择不安装——段落 6 的内容已经在对话中输出了，随时可以手动复制。

┌───────────────────────────────────────────────────────────────────────────┐
│  工具          项目级配置文件                  全局级配置文件                │
│  Claude Code   项目根目录 CLAUDE.md           ~/.claude/CLAUDE.md          │
│  Codex         项目根目录 AGENTS.md           ~/.codex/AGENTS.md           │
│  Cursor        .cursor/rules/*.mdc            Settings → Rules for AI      │
│  Cline         .clinerules/ 目录              ~/Documents/Cline/Rules/     │
│  Windsurf      .windsurf/rules/*.md           Settings → AI Rules          │
│  其他工具      —                              System Prompt / Instructions │
└───────────────────────────────────────────────────────────────────────────┘

用 AskUserQuestion 工具提供安装选项（multiSelect: false，单选）：
- "不安装，我先看看"：不写入任何文件，内容已在上方输出
- "写入 Claude Code (CLAUDE.md)"：追加到 ~/.claude/CLAUDE.md
- "写入所有检测到的工具"：自动写入用户机器上存在的所有 AI 工具配置
- "只复制文本，我自己粘贴"：不做任何操作

写入内容：段落 6 产出的"AI 协作指南"完整文本。

写入规则：
- 用 Write/Edit 工具追加到对应文件，不覆盖已有内容
- 追加段落标题 `## AI 协作指南（auto-generated by ai-interaction-analyzer）`
- 如果文件已有该段落标题，替换该段落内容（不重复追加）
- 每个工具写入后报告完整路径
```

---

## 固定输出规则

### 内容规则
1. **报告直接在对话中输出**。只有用户要求时才保存到文件。
2. **Incident 分析必须基于真实上下文**。没读上下文就不要泛化规则 — 宁可少输出也不要编造。
3. **每条规则必须溯源到具体 incident**。禁止不可追溯的泛泛建议如"提升 prompt 质量"。
4. **正负面都要分析**。只看问题不看亮点是片面的。
5. **语境感知**：`git commit`、`continue`、`ok` 在对话中完全正常，不是问题。
6. **维度拆分仅在差异显著时展示**。不要默认输出按项目的表格。
7. **Quick Install 不绑定工具**。列出所有主流 AI 工具的配置路径，让用户选。
8. **规则必须泛化**。从具体 incident 抽象到模式层面。定制发现单独列出。
9. **不要偷懒**。Layer 2 必须覆盖所有高密度 session（信号 ≥ 3），不能只看 2-3 个就停。

### 视觉规则（终端渲染方案）

核心约束：报告在 Claude Code 里是 markdown code block 渲染，CJK 双宽字符会导致 box-drawing
右边框对不齐。方案：**section 1-4 用左边框开放框（只有 ┌─ 和 │，不闭合右边框）；
section 5-8 用轻量卡片（▎标题 + 缩进）**。

**层次标记系统**（代替边框闭合）：
- `═══` 双线 = 顶级分隔（仪表盘外框、大段之间的 `---`）
- `───` 单线 = 段内子区分隔
- `▎` 竖条 = 卡片/模式标题（section 5/5b/7）
- `┄┄` 虚线 = 案例之间的轻分隔
- `▸` 三角 = 根因/结论行
- 缩进 2 空格 = 属于上一行的详情

**Section 1-4 框式规则**：
- 用 `┌───┐` 开头 + `│` 左右边框 + `└───┘` 收尾，保留完整框结构
- 尽量对齐右边框 `│`，但**对不齐就算了，绝对不能为了对齐而截断内容**
- **HARD RULE：每行内容必须 100% 完整**——数字、模型名、百分比、句子都不能被切掉任何字符。宁可整行没有右边 `│`，也不能丢一个字
- 对齐优先级：**内容完整 >>> 右边框对齐**。如果你发现某行放不下，就让那行比框宽，右边不加 `│`
- 绝对禁止：把 "Claude Code" 截成 "laude Code"、把 "5.1%" 截成 "542%"、把句子切到一半
- 柱状图用 `█` 和 `░` 块字符，长度按比例缩放到最大 40 字符
- 数字右对齐，用空格 padding

**Section 5-8 卡片规则**：
- 不用整框，避免 CJK 撑破
- 每个模式/建议是一张独立卡片，用空行分隔
- 引用原话用 `👤` `🤖` 前缀，写完整一句话（不截断到语义不通）
- 表格用 markdown pipe 表格语法（`| col1 | col2 |`），让内容自适应宽度
- 行为规则用编号列表，每条写完整句子

**通用**：
1. 每个大段用 `---` + 编号标题分隔，方便快速扫描
2. 关键数字加粗或用 emoji 前缀标记
3. 通用规则和定制发现分开：通用规则给可复制的纯文本，定制发现标注可选

---

## 约束与反模式

<HARD-GATE>
- 纯本地分析，不上传数据，不调用外部 API
- 只读用户数据文件，禁止修改 AI 工具的配置或日志
- 不对用户做价值判断，只指出具体可改进的点
- 每个 incident 上下文限制 ±4 轮，禁止读取整个 session
</HARD-GATE>

### 反模式 1：不读上下文就泛化规则
❌ 看到 prompt "部署了还是有问题" → 泛化出"遇到困难要换思路"
✅ 读取上下文发现 AI 没读文档就写代码 → 泛化出"写代码前必须先读文档"

### 反模式 2：输出统计报告
❌ "欠信息率 58%，否定率 30%，一次成功率 50%"
✅ 具体 incident 分析 + 可溯源的规则

### 反模式 3：规则太模糊
❌ "建议提升 prompt 的具体性"
✅ "介绍概念时先给实体身份（是什么/谁做的/开不开源），再用类比"

### 反模式 4：只看负面
❌ 全是问题没有亮点
✅ 同时展示成功 session 的模式，告诉用户哪些做法要保持

### 反模式 5：绑定特定工具
❌ Quick Install 只写 CLAUDE.md
✅ 列出 Claude Code / Cursor / Codex / Cline / Windsurf 等所有工具的配置路径

### 反模式 6：规则太定制，不通用
❌ "X 项目的 AB 实验要先读 wiki 文档"
✅ "用户给了文档链接必须先读，读不到要明确告知"
定制发现（项目特有的模式）放在"定制发现"段落

### 反模式 7：偷懒只分析几个 incident
❌ 51 个问题 session 只深入分析了 3 个就出报告
✅ 覆盖所有信号 ≥ 3 的 session，循环到没有新模式为止

### 反模式 8：AI 协作指南里塞统计数据
❌ "指令式沟通（72%）""每 13 条 prompt 就有 1 条要求沉淀"
✅ "习惯直接下指令，很少给背景——你需要自己读代码补上下文"
AI 协作指南是给 AI 读的行为指令。AI 读到百分比/频次不能改变任何行为。只写结论。

### 反模式 9：改一个丢三个
❌ 收到反馈后大幅重写，把之前调好的效率数据/图表/画像都删了
✅ 增量改进：加新内容、保留已有好内容、精确更新冲突点

---

## 安装

```bash
# 一键安装（检查环境、创建软链、扫描数据源）
bash <SKILL_DIR>/scripts/setup.sh
```
